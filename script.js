const { createApp, ref } = Vue;

createApp({
  // Data / State
  data() {
    return {
     
      
 
    }
  },

  // Functions / Methods
  methods: {
   
  },

  // Runs when the app starts
  mounted() {
    console.log("Vue is active and polished.");
  }
}).mount('#app');